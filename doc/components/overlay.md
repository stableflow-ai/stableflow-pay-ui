# overlay

Internal overlay, focus trap, and floating-position helpers. Dialog, Dropdown, Tooltip, DateRangePicker, and the demo may use it. Application code should not add a new dependency on this entry.

Import `@stableflow/pay-ui/overlay`.

```ts
import { Overlay, FLOATING_LAYER_Z_INDEX } from "@stableflow/pay-ui/overlay";
```
