# table

Table layout. `columns` is a CSS grid template.

Import `@stableflow/pay-ui/table`.

```ts
import { Table, TableHeader, TableBody, TableRow, TableHead, TableCell } from "@stableflow/pay-ui/table";
```
